exra commas in array literals 
// block local variable 
//read only 'here is not protected' cant be hoisting 
//in normal way cant be re-assignment 

const fish = ['lion', ,'Angel'] 

console.log(fish) ;
// Array(3) [ "lion", <1 empty slot>, "angel" ]   

fish[1] //return undefinded 

const myList = ['home' , ,'school',] ;

the lenght of myList is  3 

// Only the last comma is ignored 

const myList = ['home', ,'school', ,];
lenght is 4 
const myList =[,'home', , 'school', ,];
lenght is 4 

//// Only the last comma is ignored 